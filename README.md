# abominations, Ruby-like iteration for python
Here's what this is all about:
![alt text](https://i.imgur.com/2oXue1H.png "Comparsion of syntax with Ruby")

## Usage:
# DON'T!

```python
IntWithTimes(int).times("code", locals(optional))
ListWithEach(iterable).each("code")
                      .each_with_object("code", object)
                      .reduce("code", object)
```
